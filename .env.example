# GitHub OAuth Configuration
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Set the Authorization callback URL to: http://localhost:5173/auth/callback (for development)
# For production, use your deployed URL + /auth/callback

VITE_GITHUB_CLIENT_ID=your_github_oauth_app_client_id

# Note: Client secret should NOT be exposed in the frontend
# For production, implement a backend proxy to handle token exchange securely
# VITE_GITHUB_CLIENT_SECRET=your_client_secret_for_backend
